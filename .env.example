# =================================================================
# GADDAMAY DOCKER ENVIRONMENT CONFIGURATION
# =================================================================
# Sesuaikan port jika di server Anda port berikut sudah digunakan aplikasi lain:

# Port Layanan Utama
AGENDA_PORT=8000
ABSENSI_PORT=8080
WA_PORT=3001
DB_HOST_PORT=3307

# URL Akses (Ganti dengan domain / IP server Anda)
APP_URL=http://localhost:8000
ABSENSI_URL=http://localhost:8080

# Kredensial Database
DB_DATABASE=gaddamay
DB_USERNAME=gaddamay_user
DB_PASSWORD=rahasia123
DB_ROOT_PASSWORD=rootpass123

# Driver WhatsApp Gateway: gowa / wa_akg / log
WA_GATEWAY_DRIVER=gowa